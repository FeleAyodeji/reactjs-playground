# to start this project

this project uses a local api and to acces it we need to run the server locally

- Run the server on a seperate terminal using \*npm run server
- Then start the app on another terminal using _npm start_
